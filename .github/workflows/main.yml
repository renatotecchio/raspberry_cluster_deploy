name: Deploy Raspberry Cluster

on:
  push:
    branches: [ "*" ]
  pull_request:
    branches: [ "*" ]

jobs:

  pre-build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: Install OpenVPN
      run: sudo apt-get install openvpn
      run: ping -c 5 e7290d195382.sn.mynetname.net
